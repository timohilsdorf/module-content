{
  "name": "everycate-module-content",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lernmodule für die EveryCate-Lernplattform – dateibasiert, Schema-validiert, per Pull Request erweiterbar.",
  "scripts": {
    "validate": "tsx schema/validate.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.23.0",
    "typescript": "^5"
  }
}
